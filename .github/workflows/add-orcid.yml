name: Add ORCID to website

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/add-orcid.yml'

permissions:
  contents: write

jobs:
  update-orcid:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test workflow
        run: echo "ORCID workflow ready"
